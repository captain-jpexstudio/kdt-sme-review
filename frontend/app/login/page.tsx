export default function LoginPage() {
  // P1: 실제 로그인 폼 + /auth/login 연동
  return (
    <main style={{ padding: 40, fontFamily: "system-ui" }}>
      <h1>Survey Web — 로그인</h1>
      <p style={{ color: "#888" }}>P1에서 구현 예정 (login → 동의 게이트 → 워크스페이스)</p>
    </main>
  );
}
